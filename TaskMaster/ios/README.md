# iOS native project

Generate this folder with:

```bash
npx react-native init TaskMaster --version 0.74.1
```

Then copy the `ios/` output here (Xcode workspace, Podfile, AppDelegate, Info.plist). Run `cd ios && pod install` before the first build.
