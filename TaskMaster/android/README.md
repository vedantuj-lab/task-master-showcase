# Android native project

Generate this folder with:

```bash
npx react-native init TaskMaster --version 0.74.1
```

Then copy the `android/` output here. It contains Gradle configuration, `MainActivity`, `MainApplication`, and manifest files — all generated by the RN CLI so they stay in sync with the installed React Native version.
